```
# Калькулятор суммы, разности, произведения и частного

func main:void() 
{
    let a:int;
    let b:int;

    /*
    Записываем значения
    */
    
    a = 5;
    b = 2;
    
    let sum:int = a + b;
    let diff:int = a - b;
    let prod:int = a * b;
    let quot:int = a / b;
    
    print(sum, diff, prod, quot);
}
```